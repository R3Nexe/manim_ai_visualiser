// Using this prompt to get the manim codes, its near perfect for lists

Act as an Expert Manim Developer and Educational UI Designer. I am building a high-quality, 1080p (16:9) Data Structures and Algorithms visualizer.

I will provide an Algorithm Name/Problem Statement and optionally the Python Code. Your task is to act autonomously: analyze the logic, infer the necessary visual data structures, extract the key tracking variables, and deduce the time complexity to generate a complete, error-free Manim Scene class.

🛑 ANTI-CRASH & LAYOUT GUARDRAILS (YOU MUST OBEY THESE):
1. NO Code() OBJECTS: Manim's built-in Code() object causes version conflicts. You must build custom code blocks using Text(..., t2c={...}).

2. PREVENT t2c SUBSTRING COLLISIONS: When mapping syntax colors, you MUST append a space to keywords to prevent overlap crashes (e.g., "def ": ORANGE, "for ": ORANGE, "in ": ORANGE). NEVER use keys that are substrings of other keys.

3. NO text_alignment PARAMETER: Never use text_alignment inside a Text() object. It throws an AttributeError.

4. STRICT QUADRANT BOUNDARIES: Elements MUST NEVER cross the grey dividing lines. Code stays strictly left of x=0. State stays strictly right of x=0. Data stays strictly above y=0.

THE 5-SCENE ARCHITECTURE RULES:
SCENE 1: Educational Intro (Full Screen)

1. Group a BOLD Title (font_size 52) and a BLUE Subtitle (font_size 36). Center them, FadeIn(shift=UP), then shift to UP, buff=1.0.

2. Display a YELLOW Prerequisite text (font_size 28) and a 1-2 sentence explanation (font_size 24) using a VGroup arranged DOWN.

3. FadeOut everything to completely clear the screen.

SCENE 2: UI Grid Generation

1. Draw h_line = Line(LEFT * 8, RIGHT * 8, color=GRAY, stroke_width=2).move_to(ORIGIN)

2. Draw v_line = Line(DOWN * 4, ORIGIN, color=GRAY, stroke_width=2)

SCENE 3: Populating UI Zones (EXACT COORDINATES REQUIRED)

1. Zone 1 (Top Half - Data): Anchor the main data structure precisely to UP * 2.5. Create compact pointers (Text with an Arrow pointing UP from DOWN * 0.5) to ensure they stay entirely above the h_line.

2. Zone 2 (Bottom Left - Code): Build a custom code block using a list of strings rendered with Text(..., font="Monospace", font_size=20, t2c={...}). Wrap it in a SurroundingRectangle(..., fill_color="#1e1e1e"). CRITICAL: Scale the group to scale(0.65). Anchor the "Code:" title to to_edge(LEFT, buff=0.5).set_y(-0.5). Anchor the code block directly below it (next_to(..., DOWN, aligned_edge=LEFT)). Create a yellow SurroundingRectangle for the highlight tracker.

3. Zone 3 (Bottom Right - State): Anchor "Execution State:" strictly to next_to(ORIGIN, RIGHT, buff=0.5).set_y(-0.5). List dynamic variables below it using distinct colors, arranged(DOWN, aligned_edge=LEFT). Anchor the exp_text safely at RIGHT * 0.5 + DOWN * 3.5.

4. Helpers: Define nested helper functions:

- move_highlight(line_idx) targeting lines_group

- update_exp(new_text_str)

- update_var(var_mob, new_text_str, color) maintaining aligned_edge=LEFT.

SCENE 4: Executing the Loop

1. Step through the algorithm logically. For every major step:

- Call move_highlight(line_idx)

- Call update_var(...)

- Call update_exp(...)

- Update the visual structure in Zone 1 (Indicate, Swap, set_fill).

- Use if statements or loops to skip redundant visual repetitions if the animation exceeds 15 steps.

SCENE 5: Educational Outro (Complexity Breakdown)

1. FadeOut ALL grid lines, code blocks, state variables, and data structures. Clear the screen.

2. Display "Time Complexity: [Big O MathTex]" in the center, then shift to UP, buff=1.5.

3. Animate 3 bullet points (font_size 28) line-by-line explaining why this specific algorithm has this complexity.

4. Conclude with an Optimization Status ("Optimization Status: [Status]" in RED or GREEN) with a 1-sentence description below it.

INPUT:
Algorithm/Problem: [INSERT NAME HERE]

Python Code:
[INSERT PYTHON CODE HERE]

Output ONLY the complete Python Manim script. Do not output any markdown text outside the code block.